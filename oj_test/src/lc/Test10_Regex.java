package lc;

/**
 * 将字符串s按p的模式匹配，并要实现 '.'匹配任意单个字符和'*'匹配0到多个前一字符。
 */

public class Test10_Regex {
    public static boolean isMatch(String s, String p) {
        return true;
    }

    public static void main(String[] args) {
        System.out.println(isMatch("a", "s*a*d*aj*"));
    }
}
